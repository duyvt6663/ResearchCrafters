# Branch Solution Notes (template)

TODO: add a markdown file per non-canonical branch describing why that
branch fails or is suboptimal. The package's stage policy hides these
files from the AI mentor until the learner has completed the package.
