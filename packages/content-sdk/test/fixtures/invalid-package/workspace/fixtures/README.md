# Fixtures (template)

TODO: document how a maintainer regenerates fixtures for replay-mode
stages. Include hardware, software, command, environment hash, git SHA,
and date fields. Compute SHA-256 with `shasum -a 256` and record in
workspace/runner.yaml.
