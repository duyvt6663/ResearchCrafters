# infra/terraform

Infrastructure-as-code for `dev`, `preview`, `staging`, `prod`. Each
environment maps to a Postgres database, Redis instance, S3 bucket prefix,
runner pool, and LLM key per `TODOS/08-infra-foundations.md`.
